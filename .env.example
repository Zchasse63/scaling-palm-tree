# Browser-safe — exposed to client bundle. Set via Netlify Dashboard for production.
NEXT_PUBLIC_SUPABASE_URL=https://bxoggqfqdwizimsltztq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_REPLACE_ME

# Server-only — never exposed to the client. Used for admin DB writes (bypasses RLS).
SUPABASE_SERVICE_ROLE_KEY=eyJ_REPLACE_ME
